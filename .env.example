# Local development: copy this to .env.local and fill in your real Atlas URI.
# Vercel: add the same MONGODB_URI in Project Settings -> Environment Variables
# for Production, Preview, and Development as needed, then redeploy.
MONGODB_URI="mongodb+srv://USERNAME:PASSWORD@CLUSTER.mongodb.net/DATABASE_NAME?retryWrites=true&w=majority"
